================================================================================
PRODUCTION CONFIGURATION TEMPLATE - COPY TO production-config.txt
================================================================================

This is a TEMPLATE file. Copy this to `production-config.txt` and fill in your
actual production values. The `production-config.txt` file is NOT committed to
Git (it contains your sensitive deployment information).

================================================================================
⚠️ CRITICAL DEPLOYMENT REMINDER
================================================================================

BEFORE EVERY CLOUD RUN DEPLOYMENT:
  1. Run: node prepareDeployment.js prepare [ConfigName]
  2. Verify: node verifyDeployment.js (optional but recommended)
  3. Deploy to Cloud Run
  4. After deployment completes: node prepareDeployment.js restore

If you skip step 1, deployment will upload ALL PDFs/PNGs and take FOREVER!

================================================================================
SETUP INSTRUCTIONS
================================================================================

1. Copy this file to `production-config.txt` in the same directory
2. Fill in all values in the configuration sections below
3. Use the deployment scripts: `node updateServerFromConfig.js` and
   `node updateClientFromConfig.js`

================================================================================
PROJECT STRUCTURE
================================================================================

F:\PDFPassword\                          (Parent directory, not a Git repo)
├── production-config.template.txt      (This file - template, committed to Git)
├── production-config.txt               (Your actual config - NOT in Git)
├── PDFPasswordOverlay\                  (React app - Git repo)
│   ├── src\
│   │   ├── App.js                       (Main app with password/PDF config)
│   │   └── components\
│   ├── public\
│   ├── build\                           (Generated by npm run build)
│   ├── package.json                     (Contains "homepage" for deployment)
│   └── .gitignore
└── PDFPasswordServer\                   (Node.js Docker server - Git repo)
    ├── server.js                        (Express server with CSP/CORS/JWT)
    ├── pdfs\                            (PDF files go here)
    ├── pngs\                            (High-res PNG pages for iOS - optional)
    ├── Dockerfile
    ├── .gcloudignore                    (Ensures PDFs/PNGs are included in deploy)
    ├── .gitignore                       (Ensures PDFs/PNGs are NOT in Git)
    └── .env                             (Local dev only, not used by GCP)

DEPLOYMENT FLOW:
1. PDFPasswordServer deployed to GCP Cloud Run (serves PDF with JWT auth)
2. PDFPasswordOverlay built locally, then uploaded to cPanel (client UI)
3. React app requests JWT from Cloud Run server, then loads PDF in iframe

================================================================================
PDF RENDERING MODES
================================================================================

This application uses THREE different rendering strategies depending on the
device type and server configuration:

MODE A: NATIVE BROWSER PDF VIEWER (Default for Desktop/Android)
  - Platforms: Desktop browsers (Chrome, Firefox, Edge, Safari) and Android
  - Method: PDF displayed using browser's native PDF viewer in <iframe>
  - Benefits: Fast loading, native zoom/scroll controls, minimal JS overhead
  - Quality: Native PDF rendering quality
  - Activation: Automatic for non-iOS devices

MODE B: PDF.js CANVAS RENDERER (Default for iOS)
  - Platforms: iOS devices (iPad, iPhone) when SERVE_PNGS_FOR_IOS=false (default)
  - Method: PDF rendered to <canvas> elements using Mozilla's PDF.js library
  - Why: iOS Safari has limitations embedding PDFs in iframes
  - Benefits: Works on iOS, page preloading, fully scrollable
  - Quality: PDF.js applies some compression; good but not lossless
  - Activation: Automatic for iOS devices when PNG mode is disabled

MODE C: HIGH-RESOLUTION PNG IMAGES (Optional for iOS)
  - Platforms: iOS devices (iPad, iPhone) when SERVE_PNGS_FOR_IOS=true
  - Method: Server sends PNG file array, client renders as scrollable column
  - Why: Provides superior image quality vs. PDF.js compression
  - Benefits: Lossless quality, smooth scrolling, lazy loading
  - Drawbacks: Larger file sizes, increased bandwidth usage
  - Activation: Requires PNG files + SERVE_PNGS_FOR_IOS=true env var

HOW MODE SELECTION WORKS:
  1. Client detects device type (iOS vs. other)
  2. Client sends device info + PDF filename to server during auth
  3. Server checks SERVE_PNGS_FOR_IOS flag and scans for PNG files
  4. Server responds with rendering instructions (usePngMode + pngFiles array)
  5. Client renders using the specified mode

TO ENABLE PNG MODE FOR iOS:
  1. Set SERVE_PNGS_FOR_IOS=true in Cloud Run environment variables
  2. Export PDF pages as PNG files: [basename]-1.png, [basename]-2.png, etc.
  3. Create a subdirectory in PDFPasswordServer/pngs/ named after the PDF basename
     Example: For "cover-letter-cv.pdf", create PDFPasswordServer/pngs/cover-letter-cv/
  4. Place PNGs in that subdirectory: PDFPasswordServer/pngs/[basename]/[basename]-1.png
  5. ⚠️ CRITICAL: Before deploying to Cloud Run, ALWAYS run:
     node prepareDeployment.js prepare [ConfigName]
     This filters out PDFs/PNGs from other deployments to speed up upload!
  6. Deploy to Cloud Run (PNGs are automatically included)
  7. After deployment completes, restore files:
     node prepareDeployment.js restore

================================================================================
DEPLOYMENT CONFIGURATIONS
================================================================================

This file supports multiple deployment configurations. Each configuration is
a separate deployment (e.g., proposal, cover letter/CV, etc.).

To add a new deployment:
1. Copy an existing configuration section (e.g., "=== ExampleProject ===")
2. Rename it (e.g., "=== MyNewProject ===")
3. Update all values within that section
4. Use the config name when running deployment scripts

⚠️ IMPORTANT DISTINCTION:
   - The Cloud Run URL contains a number that is NOT the Project ID
   - Always use GCP Project ID for gcloud commands
   - These numbers are different and serve different purposes

================================================================================
=== ExampleProject ===
================================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VALUES YOU NEED TO CHANGE MANUALLY (Fill these in before deployment)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. PDF Filename:
   your-document.pdf

2. Password (for Cloud Run server only):
   your-password-here

3. Production Subdirectory:
   your-subdirectory

4. Staging Subdirectory (nested under Production):
   stage

5. Cloud Run Service Name:
   your-service-name

6. Serve PNGs for iOS (true/false):
   false
   ⚠️ When true: iOS devices get high-res PNG images instead of PDF.js
   ⚠️ When false: iOS devices use PDF.js renderer (default)
   ⚠️ Requires PNG files in PDFPasswordServer/pngs/ directory

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VALUES USUALLY SAME/REUSED (Change only if different from previous deployment)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

7. Allowed Origin (for CORS and CSP frame-ancestors):
   https://yourdomain.com

8. GCP Project ID:
   your-gcp-project-id
   ⚠️ IMPORTANT: Use THIS number for gcloud commands, NOT the number in the URL!

9. GCP Region:
   us-west2

10. JWT Secret:
    generate-a-long-random-secret-key-here
    ⚠️ Can be reused from previous deployments for convenience

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VALUES AUTO-UPDATED BY DEPLOYMENT PROCESS (AI will update these)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

11. Cloud Run URL:
    https://your-cloud-run-service-url.run.app
    ⚠️ WARNING: The number in this URL is NOT the Project ID!
    ⚠️ This is the Cloud Run service's subdomain, which may differ from the project.
    ⚠️ This value will be updated automatically after Cloud Run deployment completes.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INFORMATIONAL (No action needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

12. PNG Naming Scheme (informational):
    pngs/[PDF-basename]/[PDF-basename]-[pageNumber].png
    ⚠️ This is AUTOMATIC - you don't configure it, just follow this format
    ⚠️ Example: If PDF is "your-document.pdf":
       - Create directory: PDFPasswordServer/pngs/your-document/
       - Place PNGs inside: your-document-1.png, your-document-2.png, etc.
    ⚠️ PNGs must be in a subdirectory named after the PDF basename
    ⚠️ Page numbers should start at 1 and be sequential (no gaps)

NOTE: When staging, the full path will be: [Production Subdirectory]/[Staging Subdirectory]/
      Example: https://yourdomain.com/your-subdirectory/stage/

================================================================================
PREREQUISITES FOR DEPLOYMENT
================================================================================

Before deploying, ensure the following are set up:

1. Docker Desktop is installed and running
2. Google Cloud CLI (gcloud) is installed
3. Authenticated with GCP: gcloud auth login
4. Correct GCP project is active: gcloud config set project [GCP Project ID]
5. Required GCP APIs are enabled (Cloud Run, Cloud Build, Artifact Registry)

Note: If APIs aren't enabled, the deployment command will prompt you to enable them.

================================================================================
USING THE DEPLOYMENT SCRIPTS
================================================================================

The deployment scripts automatically read values from production-config.txt
and update your code files before building/deploying.

UPDATE SERVER CODE:
  node scripts/updateServerFromConfig.js update [ConfigName]
  node scripts/updateServerFromConfig.js revert

UPDATE CLIENT CODE:
  node scripts/updateClientFromConfig.js update [ConfigName]
  node scripts/updateClientFromConfig.js update [ConfigName] --staging
  node scripts/updateClientFromConfig.js revert

If production-config.txt is missing, the scripts will provide instructions
on how to set it up using this template file.

================================================================================
COMMON USER COMMANDS
================================================================================

### Deploy a New Full-Stack Service

**Scenario:** User wants to deploy a completely new PDF service (new PDF/PNGs, new Cloud Run URL, new React app subdirectory).

**WHEN USER SAYS:** "deploy a new full-stack service" or "deploy new service" or "create new deployment"

**Prerequisites:**
1. Add a new configuration section to production-config.txt (e.g., === MyNewProject ===)
2. Fill in values 1-6 manually (PDF filename, password, subdirectory, service name, PNG mode)
3. Place your PDF in PDFPasswordServer/pdfs/ and PNGs (if needed) in PDFPasswordServer/pngs/[basename]/
4. Values 7-10 will be reused from previous deployments (or update if different)
5. Value 11 (Cloud Run URL) will be auto-updated after deployment

**Workflow:**
1. Prepare deployment (filter files to only include this project's PDF/PNGs):
   node prepareDeployment.js prepare [ConfigName]

2. Verify what will be uploaded (optional but recommended):
   node verifyDeployment.js

3. Update server code with config values:
   node updateServerFromConfig.js update [ConfigName]

4. Navigate to server directory and deploy to Cloud Run:
   cd ../PDFPasswordServer
   gcloud config set project [VALUE 8: GCP Project ID] --quiet
   gcloud run deploy [VALUE 5: Cloud Run Service Name] --source . --platform managed --region [VALUE 9: GCP Region] --allow-unauthenticated --quiet

5. Get the new Cloud Run URL:
   gcloud run services describe [VALUE 5: Cloud Run Service Name] --region [VALUE 9: GCP Region] --format="value(status.url)"

6. Update config value 11 (Cloud Run URL) with the URL from step 5
   (AI will update this automatically)

7. Set environment variables on Cloud Run:
   gcloud run services update [VALUE 5: Cloud Run Service Name] --region [VALUE 9: GCP Region] --update-env-vars="PDF_PASSWORD=[VALUE 2: Password],JWT_SECRET=[VALUE 10: JWT Secret],ALLOWED_ORIGINS=[VALUE 7: Allowed Origin],SERVE_PNGS_FOR_IOS=[VALUE 6: Serve PNGs for iOS]" --quiet

8. Restore all PDFs/PNGs from backup (now that deployment is complete):
   cd ../PDFPasswordDevOps
   node prepareDeployment.js restore

9. Update React app with config values:
   node updateClientFromConfig.js update [ConfigName]

10. Build React app (with clean):
    cd ../PDFPasswordOverlay
    npm run build:clean

11. Upload build folder contents to cPanel via FTP:
    Upload to: public_html/[VALUE 3: Production Subdirectory]/

**Note:** The AI assistant can automate most of these steps for you. Just say "deploy a new full-stack service" and provide the config name, or update values 1-6 in the config first.

### Update Existing Service

**Scenario:** User wants to update an existing service (e.g., new PDF, password change, etc.)

**WHEN USER SAYS:** "update the PDF" or "deploy PDF to Cloud Run" or "push new PDF" or "update existing service"

1. Update values in production-config.txt for the existing config section
2. Prepare deployment (filter files):
   node prepareDeployment.js prepare [ExistingConfigName]
3. Follow steps 3-11 from "Deploy a New Full-Stack Service" above

### Build for Production

**Scenario:** User wants to build and deploy the React app to production subdirectory

**WHEN USER SAYS:** "build for production" or "deploy to production"

1. Update React app with config values:
   node updateClientFromConfig.js update [ConfigName]

2. Build React app (with clean):
   cd ../PDFPasswordOverlay
   npm run build:clean

3. Upload build folder contents to cPanel via FTP:
   Upload to: public_html/[VALUE 3: Production Subdirectory]/

4. (Optional) Revert React app to white-label:
   cd ../PDFPasswordDevOps
   node updateClientFromConfig.js revert

### Build for Staging

**Scenario:** User wants to build and deploy the React app to staging subdirectory

**WHEN USER SAYS:** "build for staging" or "deploy to staging"

1. Update React app with staging config values:
   node updateClientFromConfig.js update [ConfigName] --staging

2. Build React app (with clean):
   cd ../PDFPasswordOverlay
   npm run build:clean

3. Upload build folder contents to cPanel via FTP:
   Upload to: public_html/[VALUE 3: Production Subdirectory]/[VALUE 4: Staging Subdirectory]/

4. (Optional) Revert React app to white-label:
   cd ../PDFPasswordDevOps
   node updateClientFromConfig.js revert

### Revert to White-Label

**Scenario:** User wants to revert code changes back to white-label state

**WHEN USER SAYS:** "revert to white-label" or "revert code"

1. Revert server code:
   node updateServerFromConfig.js revert

2. Revert client code:
   node updateClientFromConfig.js revert

================================================================================

